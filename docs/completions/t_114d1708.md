# Completion Evidence: t_114d1708 — CDP Bazaar HTTPFacilitatorClient

## Completed Work

| # | Item | Path | Status |
|---|------|------|--------|
| 1 | CDP-JWT facilitator module | `src/facilitator.ts` | ✅ |
| 2 | Worker import of facilitator | `src/index.ts:11` | ✅ |
| 3 | CDP secrets in wrangler.toml | `wrangler.toml` | ✅ |
| 4 | declareDiscoveryExtension on routes | `src/index.ts` (~lines 690–781) | ✅ |
| 5 | Typecheck passes | `npx tsc --noEmit` → 0 errors | ✅ |
| 6 | Scan route seed script | `seed_raw_cdp_scan.js` | ✅ |
| 7 | Route seed status doc | `BAZAAR_SEED_STATUS.md` | ✅ |

## Verification Outputs

**Acceptance check:**
```
$ cd /home/rez/projects/x402-data-api
$ [ -f src/facilitator.ts ] && head -5 src/facilitator.ts | grep -q "HTTPFacilitatorClient"
→ PASS
```

**Typecheck:**
```
$ npx tsc --noEmit
→ 0 errors, clean exit
```

**Dual-rail check (xpay default):**
- `src/facilitator.ts`: `createCdpFacilitator()` called only via `selectResourceServer()` which checks `FACILITATOR_MODE`
- Default `FACILITATOR_MODE = "xpay"` — CDP facilitator only activates when explicitly set to `"cdp"`
- Backward compatible: existing xpay settle path unchanged

**Secret hygiene:**
- No credentials in source code
- `env.CDP_API_KEY_ID` and `env.CDP_API_KEY_SECRET` from Worker secrets (`wrangler secret put`)

## Blocked: Buyer Wallet Underfunded

```
Buyer address: 0xC4852c26498d3187dEc2ce1b19e840710e302d1e
ETH balance: 0
USDC balance: 0.002 (2,000 wei on Base)
Required for /scan/mcp seed: 100,000 wei USDC ($0.10) + ~0.0005 ETH gas
```

Run `node seed_raw_cdp_scan.js` after funding the buyer wallet. The `/pm/markets` route ($0.005 = 5,000 wei) would also fail at the current balance.
